numMetros = int(input("Digite a quantidade de metros: "))
numCent = numMetros * 100
print(f'O tamanho {numMetros}m² equivale a {numCent}cm².')

# Entrada
# A = lista1, lista2
# n = 2
# x = numMetros, numCent
# Saida
# 1 Ajustar a reposta para NOT FOUND
# 2 Para cada indice i em A:
#   Se A[i] = x de cada lista e se lista1[i]*100 = lista2[i], ajustar i como resposta
# 3 Retornar a resposta como saida.