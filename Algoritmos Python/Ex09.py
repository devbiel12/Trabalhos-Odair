reais = int(input('Digite o valor em reais: '))
dolar = reais/5.71
print(f'A quantidade de dolares que o usuario conseguira comprar com R${reais} é ${dolar}')

# Entrada
# A = lista1, lista2
# n = 2
# x = reais, dolar
# Saida
# 1 Ajustar a reposta para NOT FOUND
# 2 Para cada indice i em A:
#   Se A[i] = x de cada lista e se lista1[i]/5,71 = lista2[i], ajustar i como resposta
# 3 Retornar a resposta como saida.